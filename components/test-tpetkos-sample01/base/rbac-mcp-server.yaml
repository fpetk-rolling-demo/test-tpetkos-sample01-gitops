# RBAC resources for Kubernetes MCP Server
# The MCP Server needs read access to Kubernetes resources to provide
# cluster introspection tools to the AI agents
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: test-tpetkos-sample01-mcp-sa
  labels:
    app.kubernetes.io/instance: test-tpetkos-sample01
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: test-tpetkos-sample01-mcp-sa
    app.kubernetes.io/component: mcp-server
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: test-tpetkos-sample01-mcp-reader
  labels:
    app.kubernetes.io/instance: test-tpetkos-sample01
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: test-tpetkos-sample01-mcp-reader
rules:
  # Core resources - read-only access
  - apiGroups: [""]
    resources:
      - namespaces
      - pods
      - pods/log
      - services
      - events
      - configmaps
      - secrets
      - persistentvolumeclaims
      - nodes
      - endpoints
      - replicationcontrollers
    verbs: ["get", "list", "watch"]
  # Apps resources - read-only access
  - apiGroups: ["apps"]
    resources:
      - deployments
      - replicasets
      - statefulsets
      - daemonsets
    verbs: ["get", "list", "watch"]
  # Batch resources - read-only access
  - apiGroups: ["batch"]
    resources:
      - jobs
      - cronjobs
    verbs: ["get", "list", "watch"]
  # Networking resources - read-only access
  - apiGroups: ["networking.k8s.io"]
    resources:
      - ingresses
      - networkpolicies
    verbs: ["get", "list", "watch"]
  # OpenShift routes - read-only access
  - apiGroups: ["route.openshift.io"]
    resources:
      - routes
    verbs: ["get", "list", "watch"]
  # Metrics - read-only access
  - apiGroups: ["metrics.k8s.io"]
    resources:
      - pods
      - nodes
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: test-tpetkos-sample01-mcp-reader-binding
  labels:
    app.kubernetes.io/instance: test-tpetkos-sample01
    app.kubernetes.io/managed-by: kustomize
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: test-tpetkos-sample01-mcp-reader
subjects:
  - kind: ServiceAccount
    name: test-tpetkos-sample01-mcp-sa
    namespace: rhdh-app

